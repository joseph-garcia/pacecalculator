<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        tools:showIn="navigation_view">

    <!-- Only select one menu item within this group -->
    <group android:checkableBehavior="single">
        <item
                android:id="@+id/ic_row"
                android:icon="@drawable/ic_row"
                android:title="Rowing"></item>
        <item
                android:id="@+id/ic_loop"
                android:icon="@drawable/ic_loop"
                android:title="Looping"></item>
    </group>

    <!-- these won't stay selected when we choose them, but
    they will instead execute an action -->

    <!--
    <item android:title="TitleHere">
        <menu>
            <item android:id="@+id/nav_share"
                android:icon="@drawable/idhere"
                android:title="stringHere"></item>
        </menu>
    </item>

    -->

</menu>